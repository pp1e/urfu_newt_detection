[tool.poetry]
name = "newt_detection"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"

[[tool.poetry.source]]
name = "torch-cpu"
url = "https://download.pytorch.org/whl/cpu"
priority = "explicit"

[tool.poetry.dependencies]
python = "3.12.*"
opencv-python = "^4.12.0.88"
numpy = "^2.2.6"
torch = { version = "2.5.1", source = "torch-cpu" }
torchvision = { version = "0.20.1", source = "torch-cpu" }
#torch = { version = "^2.9.0", source = "torch-cpu" }
#torchvision = "^0.24.0"
#torchvision = { version = "^0.24.0", source = "torch-cpu" }  # Добавьте source = "torch-cpu"
aiogram = "^3.22.0"
segmentation_models_pytorch = "^0.5.0"
albumentations = "^2.0.8"
python-dotenv = "^1.2.1"
timm = "0.9.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
